# HAPPA TRADEMART — local environment configuration
# The Supabase project URL and API key. The key was only stored in this file
# and was lost during testing cleanup; paste it from the Supabase dashboard:
#   Supabase → Project (lifsgstqkkzhkbafuodp) → Settings → API → Project API keys
# (If you don't restore the key, the app keeps working in local db.json mode.)

SUPABASE_URL=https://lifsgstqkkzhkbafuodp.supabase.co
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImxpZnNnc3Rxa2t6aGtiYWZ1b2RwIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODM2MTc4MjksImV4cCI6MjA5OTE5MzgyOX0.fGKieuxaUeerDZeaqNYa7ZYJukTP2yTc_GUEkkayXfA

# ── WhatsApp Cloud API (Meta) — vendor order notifications ──────────
# WHATSAPP_PHONE_NUMBER_ID=<your Meta WhatsApp phone number ID>
# WHATSAPP_ACCESS_TOKEN=<permanent system user access token with whatsapp_business_messaging permission>
# WHATSAPP_API_VERSION=v22.0
# ADMIN_BASE_URL=<base URL of your admin panel, e.g. https://app.happatrademart.com>
# WHATSAPP_ENABLED=true   # set to false to skip real API calls (logs status "skipped")

# Session signing secret (server-side only). Required on the deployed backend so
# login tokens survive serverless instance rotation. Generate one with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# SESSION_SECRET=replace-me-with-a-long-random-hex-string
